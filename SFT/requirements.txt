# Google Colab / CUDA 训练（Llama 3.x + LoRA + TRL）
# 安装: pip install -r requirements.txt
torch>=2.2.0
transformers>=4.44.0
datasets>=2.19.0
accelerate>=0.33.0
peft>=0.12.0
trl>=0.9.0
sentencepiece>=0.2.0
protobuf>=4.25.0
bitsandbytes>=0.43.0; sys_platform != "darwin"
